# Local development can leave this empty because Vite proxies /api to the backend.
# In Vercel, set this to your Render backend URL, for example:
# VITE_API_URL=https://analise-tct-api.onrender.com
VITE_API_URL=
